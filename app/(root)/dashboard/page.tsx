export async function generateMetadata() {
  return {
    title: "Dashboard - hyre.one",
    description: `Resume shared with hyre.one. You can share your's too. Share now.`,
  };
}

export default function Dashboard() {
  return <p>Dashboard</p>;
}
